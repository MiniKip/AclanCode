// I already have planned what I will have to write.

import {
  bg,
  BoxRenderable,
  brightWhite,
  createCliRenderer,
  fg,
  StyledText,
  t,
} from "@opentui/core";
import { makeChatBits } from "./parts/chat";
import { makeFooterBits } from "./parts/footer";
import { makeHeaderBits } from "./parts/header";
import { makePromptBits } from "./parts/prompt";
import { makeWelcomeBits } from "./parts/welcome";
